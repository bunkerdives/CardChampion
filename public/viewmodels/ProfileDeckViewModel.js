var ProfileDeckViewModel = function() {
    
    
    
    this.initProfileDeckView = function( deck ) {
        
        if( deck == undefined ) {
            return;
        }
        
    }
    
};